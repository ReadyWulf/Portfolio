/// @desc gets and returns the Current spring force of the spring provided;
/// @func spring_get_spring_force_x(spring_array);
/// @arg spring_array : The spring to get data from

var spr = argument0;

return spr[15];