/// @desc gets and returns the angle of the spring provided;
/// @func spring_get_angle(spring_array);
/// @arg spring_array : The spring to get the angle from

var spr = argument0;

return spr[23];