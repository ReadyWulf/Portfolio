/// @desc gets and returns the Current length of the spring provided;
/// @func spring_get_length(spring_array);
/// @arg spring_array : The spring to get the length from

var spr = argument0;

return spr[24];