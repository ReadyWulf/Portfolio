/// @desc gets and returns the Current total force of the spring provided;
/// @func spring_get_total_force_y(spring_array);
/// @arg spring_array : The spring to get data from

var spr = argument0;

return spr[20];